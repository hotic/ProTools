# ProTools 环境变量配置

# LLM Provider 配置
# 可选值：openai | gemini
# 支持逗号分隔配置多个，如：openai,gemini（调用方会并发调用）
LLM_PROVIDER=gemini

# OpenAI API Key（使用 OpenAI GPT-5.2 时必需）
# 获取地址：https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI reasoning effort 级别
# 可选值：none | low | medium | high | xhigh（默认 xhigh）
# 设为 none 关闭推理，加快响应速度
OPENAI_REASONING_EFFORT=xhigh

# Google Gemini API Key（使用 Gemini 3 Flash 时必需）
# 获取地址：https://aistudio.google.com/app/apikey
GEMINI_API_KEY=AIzaxxxxxxxxxxxxxxxxxxxxxxxx

# Gemini thinking level 级别
# 可选值：NONE | LOW | MEDIUM | HIGH（默认 HIGH）
# 设为 NONE 关闭思考，加快响应速度
GEMINI_THINKING_LEVEL=HIGH

# 是否在审查完成后询问用户反馈
# 设置为 true 或 1 启用
ASK_USER_FEEDBACK=false

# 是否启用自动并发审查
# 开启后，工具会自动并发调用 LLM_PROVIDER 中所有配置的模型
# 关闭时，工具只调用单个模型，并返回配置的 provider 列表供调用方决定
# 设置为 true 或 1 启用
CONCURRENT_REVIEW=false
